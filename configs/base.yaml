project:
  name: "MedChange-VLM"
  version: "0.1.0"
  seed: 42

environment:
  device: "auto"
  precision: "auto"

data:
  sample_dir: "data/samples"

image:
  size: 448
  channels: 3
  normalize: false
  preserve_aspect_ratio: true

model:
  name: null

logging:
  level: "INFO"